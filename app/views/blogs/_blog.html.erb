<div class="container">
<div id="<%= dom_id blog %>">
  <p>
    <strong>Title:</strong>
    <%= blog.title %>
  </p>

  <h6><%= pluralize(blog.views, "view") %></h6>

  <p>
    <strong>Body:</strong>
    <%= blog.body %>
  </p>

</div>
</div>